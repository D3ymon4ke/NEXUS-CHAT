{
  "name": "nexus-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && (cp -r frontend/dist ./dist 2>/dev/null || true)",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix frontend run dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "web-push": "^3.6.7"
  }
}
